# General
alias vim="nvim"
alias wk="cd ~/src/work"
alias itsmine="sudo chown -R $USER:$USER ."
alias socks-tunnel="ssh -D 9000 root@server"
alias venv="source .venv/bin/activate"

# SRE
alias kubectl-disconnect="kubectl config unset current-context"
alias tfmt="terraform fmt"
alias tfsw="tfswitch --bin=$LOCAL_BIN"
alias wincred-docker="echo '{\"credsStore\": \"wincred.exe\"}' > ~/.docker/config.json"
